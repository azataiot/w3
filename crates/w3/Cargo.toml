[package]
name = "w3"
description = "Git worktree discovery for the w3 CLI"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"

[package.metadata.release]
tag = false
